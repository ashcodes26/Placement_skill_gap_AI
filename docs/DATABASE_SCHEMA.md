# Database Schema

MVP database: SQLite using SQLAlchemy.

Core entities: students, student_skills, jobs, job_skills, skill_gaps, roadmap_items, assessments, import_jobs.

Foreign keys and stable identifiers must be enforced. AI-derived output must not be the only source of truth.
