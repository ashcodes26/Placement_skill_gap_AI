# AI/ML Specification

AI responsibilities include job-description skill extraction, resume skill extraction, normalization assistance, roadmap generation, interview-question generation, and natural-language explanations.

Deterministic responsibilities include validation, duplicate detection, database integrity, API schemas, permissions, final score calculation, import status, and error classification.

AI output must be schema-validated and have a safe fallback.
